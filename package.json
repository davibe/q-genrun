{
  "name": "q-genrun",
  "version": "1.0.1",
  "description": "Q.js based function that turns generators into promises",
  "main": "lib/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "./node_modules/.bin/coffee -o lib -c src"
  },
  "keywords": [
    "Q.js",
    "promises",
    "lib",
    "generators"
  ],
  "author": "Davide Bertola <dade@dadeb.it>",
  "license": "ISC",
  "peerDependencies": {
    "q": "*"
  },
  "devDependencies": {
    "coffee-script": "^1.10.0"
  }
}
